name: Ubuntu

on: [push, pull_request, workflow_dispatch]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3
    - name: Configure
      run: |
        mkdir build
        cd build
        cmake ..
    - name: Build
      run: cmake --build .

